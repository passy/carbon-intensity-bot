language:
    node_js

node_js:
    - "11"
    - "lts/*"

branches:
    only:
        - master
        - /^greenkeeper/.*$/

cache:
    directories:
        - node_modules

script:
    - cd functions
    - yarn
    - yarn build
    - yarn test
